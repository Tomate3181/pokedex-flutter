name: pokedex_flutter
description: A modern Pokedex created in Flutter with PokeAPI.
publish_to: 'none'
version: 1.0.0+1
environment:
  sdk: '>=3.0.0 <4.0.0'
dependencies:
  flutter:
    sdk: flutter
  http: ^1.2.0
  palette_generator: ^0.3.3+3

flutter:
  uses-material-design: true
